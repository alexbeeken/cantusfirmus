<h1> Examples </h1>

<div class="container">
  <ul>
    <% @list.each do |example| %>
      <li>
        <%= link_to "Notes: #{example.notes} id: #{example.id}", example_path(example.id), :method =>"Get" %>
      </li>
    <% end %>
  </ul>
</div>

<%= link_to "Generate New Example", new_example_path, :method =>"Get" %>
